- name: Stop
  hosts: all
  become: true

  tasks:
    - name: Stop redmine docker container
      community.docker.docker_container:
        name: "{{ redmine_container_name }}"
        state: stopped

    - name: Remove redmine docker container
      community.docker.docker_container:
        name: "{{ redmine_container_name }}"
        state: absent
